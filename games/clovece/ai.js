(function(root,factory){const api=factory();if(typeof module==="object"&&module.exports)module.exports=api;root.LudoAI=api;})(typeof window!=="undefined"?window:globalThis,function(){"use strict";
const WEIGHTS={easy:[1.25,1.25,1,1,1,1],medium:[1,1,1,1,1,1],hard:[1,1,1.15,1.15,1.15,1.15]};
function weightedRoll(difficulty="medium",random=Math.random){const w=WEIGHTS[difficulty]||WEIGHTS.medium,total=w.reduce((a,b)=>a+b,0);let pick=random()*total;for(let i=0;i<6;i++){pick-=w[i];if(pick<0)return i+1;}return 6;}
function chooseMove(moves,difficulty="medium",random=Math.random){if(!moves.length)return null;if(difficulty==="easy")return moves[Math.floor(random()*moves.length)];const scored=moves.map(move=>{let score=0;if(move.finishes)score+=1000;if(move.entersHome)score+=difficulty==="hard"?500:200;if(move.captures?.length)score+=difficulty==="hard"?350:300;if(move.deploys)score+=150;if(difficulty==="hard"){if(move.leavesDanger)score+=120;score-=move.risk*30;score+=(40-Math.max(0,move.fromSteps||0))*2;}else score+=(move.toSteps||0);return{move,score};});const best=Math.max(...scored.map(x=>x.score)),options=scored.filter(x=>x.score===best);return options[Math.floor(random()*options.length)].move;}
return{WEIGHTS,weightedRoll,chooseMove};});
